# Validation — SDP-CRE-040

## Scope and learning boundary

This record covers only `topic/SDP-CRE-040`, its unit tree, and the matching progress row.
`INIT_START` is `33eb230e921b2abcc5e1fab1dbadd061bd2841d7`, recorded after a clean tracked/untracked
status. Artifact state is Draft during initialization; learning remains Not started. No learner
prediction, attempt, explanation, recall, or transfer evidence is invented.

GitHub identity preflight passed as `rahulyadev` using elevated network permission after the
restricted check reported a token/network failure. Exact no-tag main fetch succeeded, and the exact
topic branch probe returned absent (exit 2). Main, origin/main, and detached Worktree HEAD matched;
no other Worktree owned this branch. The exact branch was then created from that baseline.

The source policy, unit template, canonical entry, relevant Python mappings, progress row,
publication workflow, and predecessor quality standard were read. Source references near claims
identify authoritative pages actually read. All explanations, diagrams, code, and data are original
synthetic examples. No license change or learner solution is included.

## Browser limitation

The in-app browser rejected the direct local `file:` URL under its URL security policy. Rendering
was not performed and no bypass was attempted. Static data/accessibility/responsive contracts and
JavaScript syntax are distinct from browser rendering.

## Check record

Initialization checks executed on 2026-09-09:

| Check | Observed result |
|---|---|
| CPython 3.14.7 focused tests | 42 passed |
| CPython 3.11.16 focused tests | 42 passed |
| Strict mypy 1.20.2 | Passed all 6 non-test Python source files for both 3.11 and 3.14 targets |
| Ruff 0.16.1 | Lint passed; 16 format-eligible files compliant |
| README snippets | 11 Python blocks compiled and executed independently on both runtimes |
| Demo | Both printed `(True, True, True, True, 0)` |
| Copy probe | All five observations matched on both runtimes |
| Practice | Both printed the internal destination count and `target_complete=False` |
| Visual contract | Embedded observations, order, controls and static structure passed |
| JavaScript syntax | Node.js 24.19.0 `--check` passed |
| Repository validator | Passed including lock consistency; zero forbidden-path violations |
| Git diff | `git diff --check` passed; only this unit and its progress row changed |

The first 3.14 test run exposed a documented-versus-implemented dataclass exception difference.
After inspecting the official version-pinned source, tests assert the actual exception classes on
each tested runtime and the notes explain the discrepancy. No failed check was bypassed.

The existing Local 3.14 environment supplies exact locked tool versions. A separate locked 3.11
support environment was created under `/tmp`. All pytest, Hypothesis, mypy, Ruff, uv, coverage, and
bytecode output is redirected to `/tmp`; pytest's repository cache provider is disabled. The full
regression inherits the same mypy setting, including tests that invoke mypy internally.
No environment, cache, ZIP, transcript, credential, or raw log is included in this unit.


## Final authoring review

The initialized Draft was committed as `0f4c5e3a83ac1766d58d156233fbc7b411e51e8a`. The exact
local-only list against synchronized main matched the list since `INIT_START`: that single commit.
It was normally pushed to `topic/SDP-CRE-040` with upstream set; both ahead/behind counts were zero.
No PR or merge occurred during initialization.

Final review expanded behavioral evidence for copied container aliases, runtime subtype rejection,
mutable-rule rejection, fresh identity, a failure after partial graph copying, failed memo reuse,
custom replacement, and closures retaining mutable state. The failure-injection fixture initially
failed because it omitted the recursive memo argument; that fixture was corrected and the focused
suite rerun successfully. A first regression setup also lacked a task-owned `/tmp` parent directory;
that directory was created and the full regression restarted. Neither issue required changing other
units or bypassing a failed check.

Manual review covered the simple decision ladder, GoF creation intent versus an isolated library
call, participant boundaries, identity versus equality, topology and memo scope, custom hooks,
constructor/post-init interaction, subtype behavior, immutable replacement, stale revisions,
copying versus serialization, resource ownership, partial cleanup, concurrency and async limits,
secrets, safe observation, import boundaries, analytical performance limits, and overengineering.
The five-scenario explorer includes reading guidance and its conceptual limitations. Ten interview
prompts identify specific missing reasoning steps. The distinct dispatch lab remains unsolved and
preserves the predict → run → observe → explain → refactor → vary sequence.

Current official library prose and CPython 3.14.7 source disagree about two replacement exception
cases. The note records observed 3.11/3.14 behavior and the pinned implementation rather than
claiming an unverified first-change version. No benchmark, free-threaded stress, external-provider
integration, async cancellation test, or browser rendering is claimed.

The unrelated Local checkout's ignored `.venv`, tool caches, bytecode directories, and eleven local
foundation curriculum ZIP files were inspected and preserved. None was staged, deleted, or included
in publication. Validation runs in this dedicated Worktree, with generated state under `/tmp`.


## Final executed checks and approval

Executed on 2026-09-09 after the final test refinements:

| Check | Observed result |
|---|---|
| Focused tests on CPython 3.14.7 | 52 passed: 28 example/resource, 19 experiment/visual, 5 unsolved baseline |
| Focused tests on CPython 3.11.16 | Same 52 passed |
| Full repository regression on CPython 3.14.7 | 1,022 passed across all 67 discovered test directories, each in a separate process |
| Strict mypy for 3.11 and 3.14 targets | Both passed all 6 non-test Python source files |
| Controlled negative Protocol assignment | Both targets rejected an incompatible `clone` signature with the expected assignment diagnostic |
| Ruff lint / format | Passed / all 16 format-eligible files compliant |
| README Python fences | All 11 compiled and executed independently on both runtimes |
| Demo, probe, and unsolved starter | Executed on both runtimes with matching expected observations |
| Embedded visual data | Every field for all five scenarios matched the Python probe |
| Visual static contract / script syntax | Passed / Node.js 24.19.0 check passed |
| Browser rendering | Blocked by local URL policy; not performed or bypassed |
| Final repository validation | Passed, including exact lock check and zero forbidden-path violations |
| Final diff review | Only SDP-CRE-040 artifacts and its matching progress row; diff whitespace checks passed |

The full regression includes all previously published units; none of their files was changed.
Final validator and whitespace checks were repeated after these approval metadata edits before
commit. These are maintainer checks, not learner evidence. Artifact state is **Approved** and learning
state remains **Not started**, with all learner dates, weaknesses, and evidence cells unchanged.

Final publication is separately authorized by the task: normal push, scoped PR review, squash merge,
and safe Local main fast-forward. Exact final commit, PR, merge-tree parity, and synchronization
results are reported by the task after those operations; no future remote action is claimed here.
