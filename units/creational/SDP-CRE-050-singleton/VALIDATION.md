# Validation — SDP-CRE-050

## Scope and initialization boundary

Only `topic/SDP-CRE-050`, its unit tree, and the matching PROGRESS.md row are in scope.
`INIT_START` is `0887409590392373c923b6fb6f411cf031ab5ab9`, recorded after completely clean tracked,
staged, and untracked status. Local main, origin/main, and detached Worktree HEAD all matched this
verified published predecessor. No exact local or remote topic branch existed, and no other
Worktree owned it. The topic was created at that baseline and the dedicated task pinned.

The ordinary GitHub check reported an invalid token and network error in the restricted sandbox.
The elevated check succeeded as `rahulyadev`; exact no-tag main fetch succeeded, and the exact topic
probe returned absent (exit 2). No broader refspec, credential output, reset, or history rewrite was
used. Authentication was verified before authoring.

The canonical Singleton entry, matching tracker row, hard prerequisite rows/material, Python mapping,
unit template, source/version, copyright/license, NotebookLM, and workflow policies were read.
Initial material is Draft; learning remains Not started. There is no invented learner prediction,
attempt, recall, answer, weakness, or mastery evidence. Examples and data are original and synthetic.
The separate sensor exercise remains unsolved. No later unit files are initialized or modified.

## Executed initialization checks

Executed on 2026-09-09:

| Check | Observed result |
|---|---|
| CPython 3.14.7 focused tests | 37 passed |
| CPython 3.11.16 focused tests | Same 37 passed |
| Strict mypy, 3.11 and 3.14 targets | Both passed all 8 non-test Python files |
| Negative Reader assignment | Both targets rejected the incompatible lookup parameter with assignment diagnostic |
| Ruff lint and format | Passed; 18 eligible files formatted |
| README Python snippets | 10 compiled and executed independently on both runtimes |
| Demo | Both printed `marker True True True True`, `shared True`, `report:indigo`, `closed True` |
| Identity probe | Five observations matched on both runtimes |
| Mechanics probe | Eight fields matched on both runtimes |
| Unsolved starter | Both printed `north:ready` and `target_complete=False` |
| Visual data and static contracts | Matched every embedded observation field, IDs, controls, and guidance |
| JavaScript | Node.js 24.19.0 syntax check passed |

Initial lint review identified Python-3.12-only generic syntax guidance and two style issues. The
code keeps `Generic[T]` for 3.11 with a focused explanatory suppression; context-manager style was
corrected. No failing check was bypassed. Type checks cover production/example sources; runtime
behavior tests separately check identity, lifecycle, concurrency, and errors.

The runtime paths were inspected and exist: `/home/parry/projects/solid-and-design-pattern/.venv/bin/python`
and `/tmp/sdp-cre-040/venv311/bin/python`. Both report Py_GIL_DISABLED as false. Their environments
were not changed. All generated output uses task-owned `/tmp/sdp-cre-050` paths: Hypothesis, mypy,
Ruff, pytest temporary data, uv, coverage, bytecode, snippet files, and quality-check logs. The
complete regression must inherit MYPY_CACHE_DIR because existing tests run mypy subprocesses.
Pytest's repository cache provider is disabled. No caches, environments, raw logs, or ZIPs are staged.

## Browser limitation

The browser tool rejected the direct local `file:` URL under its URL security policy. Rendering was
not performed. No local server, alternate browser, indirect execution, or policy workaround was
attempted. Static responsive/accessibility structure and data contracts are distinct from visual
rendering, browser interaction, and assistive-technology testing.

## Review boundaries

Reviewed identity versus ownership, creation versus repeated initialization, partial publication,
copy/pickle policy, inherited caches, module naming/reload, interpreter/process limits, cache misses,
lock scope and recursion, retry side effects, cancellation cleanup, stale configuration, safe
observability, and security boundaries. Citations are near subtle claims and identify pages actually
read. The full GoF Singleton chapter was not accessed; the publisher record and authors' retrospective
were read. No copyrighted chapter, diagram, or implementation is reproduced; no license is added.

This record does not claim free-threaded stress, fork/forkserver, subinterpreter, external-driver,
slow async cleanup, repeated-cancellation, performance, or learner testing. Maintainer execution
cannot advance learning state. Final authoring approval and publication checks will be recorded
only after they have occurred.

Initialization repository validation passed, including uv lock consistency and zero forbidden-path
violations. `git diff --check` passed. Scoped review found only this unit's artifacts and the exact
PROGRESS.md Absent → Draft edit; all learning cells remain unchanged.

## Initialization publication and final review

Initialization was committed as `169e2590d071994eb08c0e44ce59e58345f323d2`. The exact local-only
commit list against synchronized main and the list since INIT_START were identical: this single
commit. A normal exact-ref push set upstream; topic and remote-tracking refs then had zero local-only
and zero remote-only commits. No PR or merge occurred during initialization.

Final review added cold first-construction concurrency in a fresh interpreter, every locally
supported pickle protocol (0–5), repeated-marker graph copying, exact root acquisition/close counts,
unused providers, repeated failed attempts, concurrent failure/retry, and async body-failure cleanup.
The child-initializer experiment now records invocation on the Child class explicitly, independently
of the returned object's type. Its observations remain the same. Source review added the official
`typing.final` contract and attributed the retrospective criticism specifically to Erich Gamma.

The complete CPython 3.14.7 repository regression passed **1,072 tests across 70 isolated test
directories**. Each directory ran in a separate pytest process with `-p no:cacheprovider` and a
per-directory `/tmp` basetemp whose parent already existed. MYPY_CACHE_DIR and all other generated
state settings were inherited for the complete run. No previously published unit was modified.
The final local probe instrumentation and test-name/protocol-selection refinements received a
fresh complete unit run on both runtimes after that regression; they did not change other units.

Final authoring checks:

| Check | Actual result |
|---|---|
| Focused CPython 3.14.7 | 50 passed: 41 examples, 4 experiment/visual, 5 unsolved baseline |
| Focused CPython 3.11.16 | Same 50 passed |
| Full CPython 3.14.7 regression | 1,072 passed across 70 isolated directories |
| Strict mypy 3.11 and 3.14 targets | Both passed 8 non-test Python files |
| Negative structural lookup contract | Rejected with expected assignment error on both targets |
| Ruff lint and formatting | Passed; 20 format-eligible files compliant |
| README Python fences | 10 compiled and executed independently on each runtime |
| Demo, identity probe, mechanics probe, unsolved starter | Matching documented results on each runtime |
| Visual | All five rows matched Python; static structure passed; Node 24.19.0 syntax passed |
| Browser rendering | Unverified: direct local URL blocked; no workaround attempted |

The Local checkout remains clean apart from its existing ignored development artifacts. Its `.venv`,
three tool-cache roots, existing bytecode directories, and eleven local foundation ZIPs were
inspected and preserved. The ZIP SHA-256 fingerprints and ignored-path inventory were recorded
under `/tmp` for comparison after synchronization. They were not added to Git or treated as blockers.

The artifact is **Approved**; learning stays **Not started** with all learner dates, weaknesses, and
evidence cells unchanged. Repository validation (including lock consistency and zero forbidden paths)
and scoped whitespace/diff review are required again after these metadata edits and before the final
commit. Final publication is explicitly authorized: normal push, exact PR review, squash merge,
main refresh, safe Local main fast-forward, then one successor task. Exact remote results are
reported by the task after execution, not predicted in this file.

Post-metadata repository validation passed, including uv lock consistency and zero forbidden-path
violations. Whitespace checks and an exact baseline comparison proved 22 scoped files and only the
Singleton artifact-state cell changed in PROGRESS.md. No untracked files remained before staging.
